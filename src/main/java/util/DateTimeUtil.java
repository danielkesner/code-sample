package util;

import model.DateTime;

public class DateTimeUtil {

    // Returns true if first is before second
    public boolean before(DateTime first, DateTime second) {


        return false;
    }
}
